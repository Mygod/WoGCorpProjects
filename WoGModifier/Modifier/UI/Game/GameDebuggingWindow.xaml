<m:GlassWindow x:Class="Mygod.WorldOfGoo.Modifier.UI.GameDebuggingWindow" xmlns:m="http://schemas.mygod.tk/wpf"
               xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" Height="450" Width="600"
               xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" Closed="StopDebugging"
               Icon="{x:Static m:CurrentApp.MainIcon}">
    <m:GlassWindow.Title>
        <MultiBinding Converter="{StaticResource TitleAliasFileNameConverter}">
            <Binding Source="{StaticResource GameDebuggingWindowTitle}" />
            <Binding Path="Alias" />
            <Binding Path="FilePath" Converter="{StaticResource FileName}" />
        </MultiBinding>
    </m:GlassWindow.Title>
    <Border Name="Status" x:FieldModifier="private" CornerRadius="5" BorderThickness="5">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <TextBox Name="OutputBox" AcceptsTab="True" AcceptsReturn="True" IsReadOnly="True" x:FieldModifier="private"
                     HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto"
                     IsReadOnlyCaretVisible="True" />
            <Button Grid.Row="1" Content="{StaticResource AbortGame}" Click="AbortGame" />
        </Grid>
    </Border>
</m:GlassWindow>
